# 🔄 SESSION RECOVERY — راهنمای ادامه‌ی کار در sandbox جدید

> این فایل برای **عامل/Agent جلسه‌ی بعدی** است. چون sandbox هر بار ریست می‌شود و **تنها بکاپ پروژه همین ریپوی گیت‌هاب است**، هر چیزی که برای ادامه لازم است اینجا و در `PLAN.md` ثبت شده. ابتدا این فایل، سپس `PLAN.md` را بخوان.

- **Repo:** https://github.com/Saeedkhoshafsar/plyr  (branch: `main`)
- **پروژه:** `automation-backend` — بک‌اند اتوماسیون مرورگر (Node.js + TypeScript)، رایگان/متن‌باز/**Self-Hosted**.
- **آخرین به‌روزرسانی این سند:** 2026-06-04 — پس از پایان استپ ۱۸ (حالت Self-Hosted تک‌کاربره `DEPLOYMENT_MODE`)، استپ ۱۹ (اسکریپت نصب تعاملی `install.sh` + نصب تک‌خطی `curl|bash` + HTTPS با Caddy + استقرار Coolify) و استپ ۲۰ (انتشار خودکار Docker image روی ghcr.io با GitHub Actions + راهنمای `docs/COOLIFY.md` برای روش Docker Image).

---

## ۱) قوانین کاری (AGENT_RULES — خلاصه‌ی اجباری)

1. اول **`PLAN.md`** را بخوان؛ از **اولین استپ ناتمام (`[ ]`)** ادامه بده.
2. اختیار کامل، **بدون سؤال**. توضیح اضافه نده — فقط ابزار را صدا بزن و کار کن.
3. هر استپ شامل ۴–۶ تسک است. وقتی **وسط یک استپ به باگ پیچیده برخوردی، آن را inline حل نکن**؛ در `PLAN.md` یادداشت کن و یک استپ با اولویت بالا **پیش از** استپ بعدی اضافه کن (الگوی استپ ۵.۵ را ببین).
4. **در پایان هر استپ:** `PLAN.md` را آپدیت کن → commit + push به گیت‌هاب → یک گزارش ۳-خطی بده → سپس **منتظر بمان** تا کاربر بگوید «ادامه».
5. این فایل (`SESSION_RECOVERY.md`) را هم در پایان هر استپ به‌روز نگه دار (بخش «وضعیت فعلی»).

---

## ۲) راه‌اندازی sandbox جدید (Setup)

```bash
# 1) کلون (اگر از قبل نیست)
cd /home/user/webapp

# 2) نصب وابستگی‌ها بدون دانلود مرورگر (سریع‌تر؛ مرورگر در صورت نیاز جدا نصب می‌شود)
SKIP_BROWSER_INSTALL=1 npm install --ignore-scripts

# 3) فایل env
cp .env.example .env    # سپس API_KEYS و ADMIN_SECRET را پر کن

# 4) بررسی سلامت کامپایل (باید 0 خطا بدهد)
npx tsc --noEmit
npm run build
```

### Redis (برای تست‌های زنده‌ی صف/قفل لازم است)
```bash
sudo apt-get update && sudo apt-get install -y redis-server
redis-server --daemonize yes --port 6379
redis-cli ping     # باید PONG بدهد
# پاک‌سازی بعد از تست:  redis-cli flushall && redis-cli shutdown nosave
```

### مرورگر (فقط اگر اجرای واقعی pipeline لازم شد)
```bash
npm run install:browser        # = playwright install chromium
```

> ⚠️ **نکات محیطی مهم (از جلسات قبل):**
> - **Docker در sandbox نصب نیست** → نمی‌توان build واقعی container زد؛ فقط اعتبارسنجی استاتیک. تست end-to-end Docker روی ماشین کاربر.
> - `dump.rdb` آرتیفکت Redis است و **gitignore شده**؛ بعد از تست پاکش کن، commit نکن.
> - دانلود از لینک AI Drive با Cloudflare 403 می‌دهد → باید header `User-Agent` مرورگر به curl اضافه شود.

---

## ۳) ⚠️ نکته‌ی بحرانی فنی: خط‌پایان CRLF

تمام فایل‌های `src/**/*.ts`, `tsconfig.json`, `package.json`, `.env.example` با **CRLF (`\r\n`)** ذخیره شده‌اند (میراث ویندوز).
- ابزار `Edit` با مچ چندخطی **شکست می‌خورد** چون `\r` را نمی‌بیند.
- **روش مطمئن:** از یک اسکریپت Python با حالت باینری استفاده کن و `\r\n` را صریح در رشته‌ها بگذار:

```python
python3 - <<'PY'
p='src/FILE.ts'
with open(p,'rb') as f: d=f.read()
old=b"...line1\r\n...line2\r\n"
new=b"...new1\r\n...new2\r\n"
assert d.count(old)==1, d.count(old)   # همیشه یکتا بودن مچ را چک کن
open(p,'wb').write(d.replace(old,new))
print("OK")
PY
```
- فایل‌های markdown جدید (`PLAN.md`, `README.md`, `docs/*`, این فایل)، و نیز همه‌ی `public/**`, `tests/**`, `*.config.ts`, **`extension/**`** (افزونه‌ی استپ ۱۳)، و **`n8n-node/**`** (پکیج n8n استپ ۱۵) **LF** هستند و با ابزار `Edit` کار می‌کنند. فقط `src/**/*.ts`, `package.json` (ریشه), `.env.example` **CRLF**اند (باید با اسکریپت پایتونِ binary-mode ویرایش شوند).

---

## ۴) معماری در یک نگاه

- **Entry:** `src/index.ts` (Express + Helmet، صف BullMQ، worker، Lua، graceful shutdown، GC).
- **Config:** `src/config.ts` (env-driven؛ `cleanEnv`، `parsePlans`، `CHROME_EXE` خالی=Chromium باندل‌شده).
- **Pipeline اجرا:** `src/pipeline.ts` (`runPipeline`, Flow Engine: if/while/try/switch).
- **Routes:** `src/Routes/` → `user.routes.ts` (/me,/run,/schedule,/quota,/jobs,/job,/cancel)، `admin.routes.ts` (/admin/*)، `health.routes.ts` (/health). Mount: user/health روی `/`، admin روی `/admin`. **`/me`** = هویت صاحب کلید (بدون strict-binding) برای login UI.
- **UI (استپ ۷):** پوشه‌ی `public/` در **روت پروژه** (نه `src/`)؛ با `express.static(path.resolve(process.cwd(),'public'))` سرو می‌شود. فایل‌ها: `index.html`، `css/styles.css`، `js/i18n.js` (فارسی/انگلیسی + RTL/LTR)، `js/api.js` (`window.API`: کلید در localStorage `ab_api_key`، `validateKey`→`/me`)، `js/app.js` (login/router/theme/health + export `window.AppUtil`)، `js/views.js` (استپ ۸ — `window.Views`: run/jobs/jobDetail/quota/schedules/admin). **ترتیب لود مهم است:** i18n → api → views → app؛ `views.js` باید `AppUtil` را lazy (تابع `U()`) بخواند چون `app.js` بعداً آن را می‌سازد. همه‌ی فایل‌های `public/**` با **LF** هستند (با Edit کار می‌کنند).
- **CORS (F5):** middleware صریح در `index.ts` با `config.CORS_ALLOWED_ORIGINS` (env کاما-جدا؛ `*` = هرجا بدون credentials؛ خالی = same-origin). CSP در helmet با `scriptSrc:'self'`/`connectSrc:'self'` ⇒ **JS باید خارجی باشد (نه inline)**.
- **Core:** `ProfileManager` (مرورگر/قفل کاربر)، `GlobalBrowser` (مرورگر مشترک Free)، `QuotaManager`، `UserManager`.
- **Validation:** `src/validation.ts` (sanitize عمیق steps) + `src/schemas.ts` (Zod envelope — استپ ۶).
- **Auth:** کاربر `x-api-key`، ادمین `x-admin-token`.
- **مدل استقرار:** پیش‌فرض **single (self-hosted تک‌کاربره)**؛ حالت `multi`/SaaS بعداً (استپ ۱۸).
- معماری مرجع کامل + مقایسه با Automa + ۲ نیاز صاحب پروژه (مرورگر تعاملی + ادغام n8n) + «مسیر زنده» همگی در بالای `PLAN.md`.

---

## ۵) وضعیت فعلی (به‌روز در پایان هر استپ)

**استپ‌های تمام‌شده:** ۱ تا ۲۰ ✅ _(استپ ۱۶ زودتر از ۱۲ انجام شد چون استپ ۱۲ به آن وابسته بود — طبق AGENT_RULE 3)_ + اصلاح کوچک hotfix: محدودکردن هشدار `API_KEYS_ENABLED` فقط به حالت `multi` (کامیت `8b963e8`) + **استپ ۲۱ (مدل دادهٔ یکنواخت آیتم‌محور) ✅** + **استپ ۲۲ (کتابخانهٔ چند-ورکفلو در UI + اتصال به CRUD) ✅** + **استپ ۲۳ (بازطراحی بصری Canvas نودبیس — دسته‌بندی/رنگ‌بندی، drag&drop، grid-snap، minimap، zoom/fit، انتخاب چندتایی/box-select، کپی/پیست، حالت‌های بصری نود) ✅**.

**🚀 فاز بزرگ بعدی (نسخهٔ نهایی ۱۰۰/۱۰۰ — استپ ۲۱–۳۲) — پلتفرم اتوماسیون مرورگر نودبیس در شأن n8n:** نقشهٔ کامل و بازنویسی‌شده در انتهای `PLAN.md` ثبت شد (پس از مطالعهٔ عمیق مستندات رسمی n8n: Node types، Data structure، Expressions/Mapping، NDV/UI، Error handling، Triggers، Data pinning). خلاصه:
- **مسئلهٔ واقعی (Positioning):** n8n برای اتصال API/سرویس عالی است اما **اتوماسیون مرورگرِ واقعی** (سایت بدون API، پشت لاگین، رندرشده با JS، کپچا/ضدبات) را خوب پوشش نمی‌دهد. ما **بازوی مرورگرِ n8n** می‌شویم: «مثل n8n حس می‌شود»، n8n با Trigger صدامان می‌زند و نتیجهٔ نودبه‌نود را لحظه‌ای پس می‌گیرد.
- **مدل اجرا:** n8n = **مدل ب** (ورکفلوی ذخیره‌شده + Trigger)؛ افزونهٔ کروم = **مدل الف** (افزونه = همان پنل، لاگین = ورود به سرور، مزیت = انتخاب مستقیم المان روی مرورگر کاربر).
- **کشف کلیدی:** بک‌اند **خیلی جلوتر از UI** است. آماده: موتور workflow کامل (`if/while/loop/foreach/switch/try/...` در `pipeline.ts`)، رویدادهای لحظه‌ای هر استپ (`step.start/done/error` با durationMs/error از `onEvent`→`LiveBus`)، کانال زندهٔ WS/SSE، ذخیرهٔ چند-ورکفلو + نسخه‌بندی (`WorkflowService` + `/workflows/:userId` CRUD + `/versions` + `/run`)، webhook خروجی HMAC، نود n8n + Trigger node، افزونهٔ پایه. **شکاف اصلی = UI.**
- **۸ درس از n8n که رعایت می‌شود:** (۱) مدل دادهٔ یکنواخت آیتم‌محور `[{json, binary?}]` و اجرای per-item، (۲) NDV سه‌ستونه INPUT|Parameters|OUTPUT با drag→Expression، (۳) Expressionها `{{ $json.x }}`/`$node["n"].json`/`$now` با حالت Fixed/Expression، (۴) حالت‌های بصری نود (خط/هالهٔ چرخان در حال اجرا → سبز موفق + تعداد آیتم/زمان → قرمز خطا + پیام)، (۵) Triggerهای متنوع (Manual/Schedule-Cron/Webhook/Form/Telegram/...)، (۶) Data pinning، (۷) Error handling حرفه‌ای (Continue/Retry On Fail + Error Workflow)، (۸) انواع فیلد غنی (string/password/multiline، number، boolean toggle، options/multiOptions، collection/fixedCollection، assignmentCollection، dateTime، code، json، filter).
- **ترتیب استپ‌ها (۲۱–۳۲):** ۲۱ مدل دادهٔ یکنواخت آیتم‌محور (زیربنا) → ۲۲ کتابخانهٔ چند-ورکفلو + اتصال به CRUD → ۲۳ بازطراحی بصری Canvas (drag&drop/Bézier/minimap/حالت‌های نود) → ۲۴ نودهای شاخه‌دار + سریالایز گراف غیرخطی → ۲۵ NDV سه‌ستونه + فیلدهای غنی + Expression/Mapping (با sandbox امن) → ۲۶ اجرای زنده + نتیجهٔ هر نود مثل n8n + پنل لاگ همیشه‌جلوی‌چشم → ۲۷ Error handling حرفه‌ای (Continue/Retry/Error Workflow) → ۲۸ Triggerها (Manual/Webhook/Schedule-Cron/Telegram) + تزریق دیتا → ۲۹ گزارش دوکاناله (webhook هر استپ + لینک زنده) → ۳۰ نود n8n غنی → ۳۱ افزونهٔ کروم = همان پنل + Element Picker + تیک/خطا روی نود → ۳۲ همگام‌سازی کاتالوگ اکشن + Templates + جمع‌بندی/مستندسازی.
- **امنیت:** ارزیابی Expression در **sandbox محدود** (allow-list توابع، بدون `process`/شبکهٔ خام) — درس از CVE-2025-68613 در n8n.
- **لایسنس:** از Automa (AGPL + لایسنس تجاری) و n8n فقط **الهام UX**؛ **هیچ کدی کپی نمی‌شود** — بازسازی با استک خالص خودمان (vanilla JS، CSP-safe).

**استپ بعدی برای شروع:** **استپ ۲۴ — نودهای شاخه‌دار و سریالایز کامل گراف غیرخطی** (پورت‌های چندگانه برای `if`(true/false)/`switch`(cases+default)/`loop·foreach`(body/done)/`try`(try/catch/finally)، سریالایز/دیسریالایز گراف غیرخطی به/از `steps[]` تو‌در‌توی بک‌اند، اعتبارسنجی گراف: نود بی‌اتصال/حلقهٔ بی‌پایان/trigger الزامی در ابتدا، + تست سریالایز-دیسریالایز شاخه‌دار و تست اعتبارسنجی). ⚠️ همان قرارداد CRLF برای `src/**` (+`package.json` ریشه + `.env.example` + `ecosystem.config.js`) و LF برای `tests/**`/`docs/**`/`public/**`/`extension/**`/`n8n-node/**`/`install.sh`/`.github/**` رعایت شود (ویرایش `src/**` با اسکریپت پایتونِ binary-mode — اسکریپت‌های throwaway را بعد از اعمال پاک کن، در ریپو کامیت نشوند).

**نکته‌ی محیطی استپ ۱۲ (Live Browser View):** مرورگر سروری در این sandbox حتی با نصب `redis-server` و libهای `libnss3/libnspr4/...` بالا نیامد (`GlobalBrowser is not available`) چون deps کامل Playwright موجود نیست — محدودیت محیطی، نه باگ کد. با این حال `GlobalBrowser.initialize` خطا را می‌بلعد و `app.listen` اجرا می‌شود، پس مسیر WS و auth کامل تست شد (روی سوکت معتبرِ `/browser/ws` پیام `error: browser_unavailable` به‌درستی emit شد). **تست استریم واقعیِ فریم‌ها باید روی Docker/ماشین کاربر انجام شود.** معماری استپ ۱۲: `/browser/ws` یک listenerِ `upgrade` مستقل دارد که فقط مسیر خودش را می‌گیرد و کنار `/live/ws` همزیست است (هر دو path-based، رگرسیون گرفته شد).

> 🧭 **نکته‌ی مهم برای استپ‌های بعدی (کاتالوگ اکشن مشترک):** از استپ ۱۱، کاتالوگ `ACTIONS` دیگر دو کپی نیست؛ به **یک ماژول مشترک** منتقل شد: `public/js/actions.js` → `window.ACTION_CATALOG = { ACTIONS, actionById, ids }`. هم `views.js` (فرم خطی) و هم `flow-editor.js` (نودها) از همان می‌خوانند (flow-editor یک `actionById` محلی دارد که برای نودهای مصنوعی مثل `__start__` مقدار `null` برمی‌گرداند). برای افزودن اکشن جدید: فقط در `actions.js` اضافه کن + پیاده‌سازی بک‌اند در `src/pipeline.ts` (قبل از بخش «43. EXTERNAL MODULE») + کلیدهای i18n `p.*` (fa+en). فرمت اکشن: `{ id, icon?, fields:[{k,label,type,ph?,options?}] }`؛ بک‌اند `{action, params}` می‌گیرد. ⚠️ نودهای کنترل‌جریان چندپورتی هنوز ساخته نشده‌اند (`toSteps()` خطی است) — کار آینده‌ی ادیتور. ترتیب لود اسکریپت: **actions** → i18n → api → flow-editor → views → app. همه‌ی `public/**` و `tests/**` و `*.config.ts` **LF**؛ `src/**/*.ts` و `package.json` **CRLF**.

**خلاصه‌ی کارهای انجام‌شده‌ی کلیدی:**
- استپ ۲: ۹ خطای TS رفع شد (import casing، UPLOADS/DOWNLOADS_DIR، redis param، ES2021+DOM lib، new Date guard، implicit any).
- استپ ۳: `CHROME_EXE` خالی=Chromium باندل، postinstall امن/skippable، README + `docs/API.md`.
- استپ ۴: `Dockerfile` (multi-stage روی playwright image)، `docker-compose.yml` (app+redis)، healthcheck `/health`.
- استپ ۵: race مدیریت active jobs ([C2] `scard` بعد از `sadd`)، هماهنگی `unhandledRejection`/`uncaughtException`.
- استپ ۵.۵: **قفل توزیع‌شده‌ی امن** (token + Lua compare-and-del در `ProfileManager`)، حذف همه‌ی `KEYS` با `scanKeys` (SCAN). تست زنده ۷ سناریو PASS.
- استپ ۶: لایه‌ی Zod (`src/schemas.ts`) برای `/run` و `/schedule`، خطای یکدست؛ تست ۱۰ سناریو PASS.
- استپ ۷: UI بخش ۱ — `public/` + static serving، login با API Key (localStorage `ab_api_key` + `GET /me`)، shell داشبورد (sidebar/topbar) RTL/LTR + i18n، اتصال به `/health`، **CORS صریح [F5]**. e2e Playwright PASS.
- استپ ۸: UI بخش ۲ — `public/js/views.js` (`window.Views`): flow builder (`ACTIONS` catalog) → `POST /run`، صفحه‌ی jobs + job detail (poll)، Quota، Schedules (list/delete)، پنل admin (`x-admin-token` → `/admin/stats`). **باگ lazy `AppUtil`:** `views.js` قبل از `app.js` لود می‌شود پس `U()` به صورت lazy resolve شد. e2e Playwright بدون خطای console PASS.
- استپ ۹: **تست و پایدارسازی (D3)** — `vitest`+`supertest`؛ `tests/unit/` (helpers ۱۴، validation ۲۲ شامل SSRF، condition-engine ۱۹ شامل regex امن، schemas ۱۲) + `tests/integration/api.test.ts` (۱۴) روی اپ Express سبک با میدل‌ورهای واقعی auth/admin بدون Redis. اسکریپت‌های `test`/`test:watch`/`check`. **`npm test` → ۸۱ تست سبز**، `npm run check` سبز. عمداً `src/index.ts` import نشد (side-effect `startServer()`).
- استپ ۱۰: **ادیتور Flow بصری node-based (الهام از Automa)** — `public/js/flow-editor.js` (`window.FlowEditor`): بوم SVG + کارت‌های HTML، drag نودها، اتصال پورت out→in، pan/zoom، حذف نود/لبه، inspector params، save/load/clear در localStorage، اجرای مستقیم `POST /run`، تبدیل دوطرفه گراف↔`steps[]`. ادغام: nav/route `editor`، اسکریپت در index.html (i18n→api→flow-editor→views→app)، کلیدهای `fe.*` (fa+en)، CSS. e2e Playwright: add→connect(start→goto→click)→JSON(۲ step)→save/load→**run(Job ID:1)**؛ بدون خطای console.
- استپ ۱۱: **افزایش اکشن‌ها به‌سبک Automa (E2/E3)** — کشف کلیدی: بک‌اند `src/pipeline.ts` از قبل **۴۰+ اکشن** داشت ولی UI فقط ۱۰ تا را نشان می‌داد. (الف) کاتالوگ مشترک `public/js/actions.js` (`window.ACTION_CATALOG`، ۱۸ اکشن) ساخته شد و `views.js`+`flow-editor.js` به آن متصل شدند (پایان دو کپی). (ب) چهار اکشن جدید به بک‌اند اضافه شد: `cookie` (getAll/get/set/clear)، `variable`/`set-variable` (op: set/regex/replace/slice/split/join/sort — regex امن ضد ReDoS)، `export-data` (csv/json → `downloads/<userId>/`، helperهای `toCsv`/`csvEscape`)، `notification`. (ج) کلیدهای i18n `p.*` جدید (fa+en)، `actions.js` اول در index.html لود می‌شود. (د) `docs/API.md` بخش کامل «کاتالوگ اکشن‌ها». **تست:** `tsc`/`build` سبز، `npm test`=**۹۱** (+۱۰ تست `export-csv.test.ts`؛ `toCsv`/`csvEscape` با `export function` قابل‌تست شدند). e2e Playwright: کاتالوگ مشترک + ۸ اکشن جدید + `POST /run`(200, Job ID:1)، بدون خطای console. اجرای واقعی pipeline در sandbox به‌دلیل نبود deps مرورگر سروری شکست می‌خورد (`GlobalBrowser unavailable`) — محدودیت محیطی، نه باگ کد.
- استپ ۱۶: **کانال زنده‌ی استاندارد (Live Channel) — دسته G1** _(زودتر از ۱۲–۱۵ چون استپ ۱۲ به آن وابسته بود)_. (الف) `src/core/LiveBus.ts`: نوع‌های رویداد `LiveEventType` (`job.start`/`log`/`step.start`/`step.done`/`step.error`/`job.done`/`job.error`)، کلاس `LiveBus` (publish→capped list ۲۰۰تایی + TTL ۳۰دقیقه + Pub/Sub؛ `getBuffer` برای replay)، و `JobLivePublisher` (شمارنده‌ی `seq` + fire-and-forget). کلیدهای Redis: `getLiveChannel`/`getLiveBufferKey`. (ب) `src/types.ts`: `onEvent?` به `AutomationContext`؛ `src/pipeline.ts`: param/destructure/context `onEvent` + emit `step.start`/`step.done`/`step.error` در `executeStepGroup`. (ج) `src/index.ts`: ساخت `liveBus`، wrap لاگ worker برای emit `log`، emit `job.start`/`job.done`/`job.error` دور `runPipeline`، پاس‌دادن `onEvent`؛ endpoint SSE `GET /live/sse/:userId/:jobId`؛ باز کردن CSP `connect-src: 'self' ws: wss:`. (د) `src/core/LiveServer.ts`: سرور WebSocket روی `/live/ws` (هندلر `upgrade`)، یک subscriber با `psubscribe live:ch:*` برای fan-out (سازگار با PM2 cluster)، replay بافر روی connect، heartbeat ping، و `authorizeLive` (env-key=admin، یا owner-match با `validateAndGetOwner`). (ه) UI: `public/js/live.js` (`window.LiveClient` با WS→fallback SSE + `window.LiveView`)، کلیدهای i18n `live.*`+`nav.live` (fa+en)، آیتم nav + route `live` در `app.js`/`views.js` + هوک `stopAll`→`LiveView.stop`. **وابستگی جدید:** `@types/ws` (devDep). **تست:** `tsc`/`build` سبز، `npm test`=**۹۸** (+۴ تست `live-bus.test.ts` integration روی Redis واقعی شامل cap+Pub/Sub، +۳ تست `live-auth.test.ts`). e2e واقعی (سرور زیر `Xvfb :99`+`DISPLAY=:99` کامل بالا آمد چون `GlobalBrowser.initialize` خطا را می‌بلعد و `app.listen` اجرا می‌شود): SSE بدون کلید=401، کلید غلط=403، کلید معتبر=200 stream؛ WS بدون/غلط کلید=403، معتبر=OPEN؛ replay buffer + live stream هر دو سبز (`E2E_PASS`)؛ UI بدون خطای console/CSP.

- استپ ۱۲: **Live Browser View + Element Picker (دسته F1)** _(۲۰۲۶-۰۶-۰۴)_. (الف) `src/core/LiveBrowser.ts`: کلاس `LiveBrowserSession` (context+page ایزوله از `GlobalBrowser.getContext()`، CDP `Page.startScreencast` jpeg q=60 با ack هر فریم، متدهای `navigate`/`click`/`scroll`/`type`/`key` روی `Input.dispatchMouseEvent`/`Input.insertText`/`keyboard.press`، `setPicker` با تزریق `PICKER_SCRIPT` + binding `__abReportPick` از `exposeBinding`، idle-TTL ۵دقیقه auto-close) و `LiveBrowserManager` (registry با cap، `create`/`destroy`/`shutdown`). (ب) `src/core/BrowserStreamServer.ts`: WS روی `/browser/ws` با `attach(server)` که listenerِ `upgrade` مستقل ثبت می‌کند و **فقط مسیر `/browser/ws`** را می‌گیرد (بقیه را رها می‌کند تا با LiveServer همزیست شود)؛ auth با `authorizeLive`؛ هر سوکت یک session؛ frameها و eventها را به‌صورت JSON می‌فرستد و دستورات `{t:...}` را می‌گیرد. (ج) `src/index.ts`: import + ساخت `liveBrowserManager`(cap=min(MAX_CONCURRENT,8)) و `browserStreamServer`، `attach` در `startServer`، و بستن در graceful shutdown. (د) UI: `public/js/browser-view.js` (`window.BrowserView`: canvas + مپ مختصات + ارسال input + Picker + کارت سلکتور با کپی/افزودن گام via `Views.addStep`)؛ `Views.addStep()` جدید در `views.js`؛ nav `browser` + route + هوک `stopAll`؛ کلیدهای i18n `bv.*`+`nav.browser` (fa+en)؛ اسکریپت در index.html (live→browser-view→views). **CSP بدون تغییر** (استپ ۱۶ از قبل `ws:` و `imgSrc data:` را باز کرده بود). **تست:** `tsc`/`build` سبز، `npm test`=**۱۰۳** (+۵ تست `live-browser.test.ts`). e2e WS: no_key=403، bad_key=403، no_userId=400، valid=OPEN، پیام `error:browser_unavailable` روی سوکت معتبر؛ رگرسیون `/live/ws` سالم؛ UI بدون خطای console/CSP. (استریم واقعی فریم محدودیت محیطی sandbox — تست روی Docker.)

- استپ ۱۳: **افزونه‌ی کمکی Chrome — Manifest V3 (دسته F2)** _(۲۰۲۶-۰۶-۰۴)_. پوشه‌ی جدید **`extension/`** (همه فایل‌ها **LF**، با ابزار `Edit` کار می‌کنند — TS کامپایل نمی‌شوند، خارج از `tsconfig`). (الف) `manifest.json` MV3: permissions storage/activeTab/scripting/tabs، host_permissions http+https، action popup، background `service_worker: background.js`، content_scripts `["content/selector.js","content/recorder.js"]` در document_idle، آیکون‌های 16/48/128 (ساخته‌شده با PIL). _نکته: `default_locale` عمداً حذف شد چون `_locales/` و `__MSG_*` نداریم._ (ب) `content/selector.js`: `window.ABSelector = {cssPath, xPath}` — **همان منطق `PICKER_SCRIPT` بک‌اند** (id shortcut، class hints، `:nth-of-type`، `CSS.escape` با fallback، سقف ۶ سطح). (ج) `content/recorder.js`: Picker (overlay هایلایت hover + گزارش بدون فعال‌سازی هندلر صفحه) + Recorder (click→`click`، input/change→`fill`، Enter→`press`، ناوبری→`goto`)؛ وضعیت در `chrome.storage.local` (`ab_picker/ab_recording/ab_steps/ab_last_url`)؛ پیام‌ها `AB_PICK_START/STOP`، `AB_REC_START/STOP`، `AB_PING` ← و `AB_PICKED`، `AB_STEP_RECORDED` →. (د) `background.js` (service worker): `sendFlow`→`POST {base}/run` با `x-api-key` و بدنه `{userId, steps, headless:true, webhookUrl?}`؛ `checkConnection`→`GET /me`؛ `relayToActiveTab`؛ `normalizeBase` (افزودن http:// + حذف اسلش انتهایی)؛ کلید **هرگز لاگ نمی‌شود**؛ fetch از داخل worker تا CORS صفحه دور زده شود. (ه) `popup/`: `popup.html`+`popup.css`(dark)+`popup.js` (CSP-safe، بدون inline): Backend (Base/Key/User+Save/Test)، Capture (Pick/Record + کارت عنصر با add click/extract/copy)، Steps (count/Clear/Send)، status. فرمت گام `{action, params}` مطابق `public/js/actions.js`. (و) `extension/README.md`: نصب load-unpacked، تنظیمات، pick/record/send، جدول مجوزها، و **نکته‌ی CORS** (background-fetch مستقل از `CORS_ALLOWED_ORIGINS`؛ توصیه `*` یا origin افزونه در صورت نیاز). **تست:** `tsc`/`build` سبز، `npm test`=**۱۰۸** (+۵ تست `extension-selector.test.ts` با fake-DOM در `vm`، بدون jsdom). قرارداد افزونه↔بک‌اند روی سرور تأیید شد: `/me`→`{success:true}`، `/run`→`{success:true, jobId}`، بدون کلید→401. تست واقعی pick/record در Chrome باید روی ماشین کاربر انجام شود (sandbox مرورگر گرافیکی ندارد).

- استپ ۱۴: **بهبود API برای ادغام n8n (دسته F3/F5)** _(۲۰۲۶-۰۶-۰۴)_. (الف) **sync mode** `POST /run?wait=true` (یا `wait=1`): helper جدید `waitForJobResult(queue, userId, jobId, maxMs, pollMs)` در `src/Routes/user.routes.ts` — تا state ترمینال (`completed`/`failed`) poll می‌کند و فایل نتیجه (`readJobFile`) را همان شکل `GET /job` برمی‌گرداند (با `waited:true`)؛ در timeout (سقف `RUN_WAIT_MAX_MS`=۶۰s، poll `RUN_WAIT_POLL_MS`=۵۰۰ms) پاسخ **۲۰۲** با `pollUrl` می‌دهد. (ب) **HMAC webhook**: util جدید `src/utils/signature.ts` (`signWebhookBody`/`verifyWebhookSignature`/`SIGNATURE_PREFIX`، با `crypto.timingSafeEqual` و گارد طول)؛ در `src/services/webhook.service.ts` بدنه **یک‌بار** سریالایز می‌شود (`rawBody`) و وقتی `WEBHOOK_SECRET` ست باشد هدرهای `X-Signature: sha256=<hex>` + `X-Webhook-Timestamp` (unix s) ضمیمه می‌شوند. (ج) **Idempotency-Key**: هدر `Idempotency-Key` روی `/run`؛ اعتبارسنجی مشترک `isValidIdempotencyKey` + کلید `getIdempotencyKey(userId,key)`→`idem:run:<user>:<key>` در `src/utils/redis-keys.ts`؛ نگاشت per-user با `SET ... EX IDEMPOTENCY_TTL_SECONDS` (۲۴h)؛ کلید تکراری → همان jobId با `idempotent:true` بدون enqueue (best-effort؛ ریسِ نادر دو درخواست هم‌زمانِ پیش از ذخیرهٔ نگاشت پذیرفته‌شده و مستند است). (د) `docs/openapi.yaml` (OpenAPI 3.0.3، ۱۰ مسیر/۹ schema). (ه) CORS [F5] از قبل بود؛ فقط `Idempotency-Key` به `Access-Control-Allow-Headers` در `src/index.ts` اضافه شد. کلیدهای جدید config: `WEBHOOK_SECRET`/`RUN_WAIT_MAX_MS`/`RUN_WAIT_POLL_MS`/`IDEMPOTENCY_TTL_SECONDS` (در `src/config.ts` + `.env.example`). **تست:** `tsc`/`build` سبز، `npm test`=**۱۲۷** (+۱۹: ۱۰ `webhook-signature.test.ts`، ۴ `redis-keys.test.ts`، ۵ `run-n8n.test.ts` که با mockِ connection/queue/UserManager/validation هندلرِ واقعی `/run` را برای dedup، sync-result، و timeout→202 می‌سنجد — بدون Redis/مرورگر). فایل‌های جدید TS (`signature.ts`, تست‌ها) همگی **CRLF برای `src/**`** و **LF برای `tests/**`** طبق قرارداد؛ `docs/openapi.yaml` و `.env.example` با اسکریپت پایتونِ binary-mode ویرایش شدند.

- استپ ۱۵: **n8n Community Node (دسته F4)** _(۲۰۲۶-۰۶-۰۴)_. پکیج **مستقل** `n8n-node/` (`n8n-nodes-automationbackend`، همه فایل‌ها **LF**، خارج از tsconfig بک‌اند). (الف) ساختار استاندارد n8n: `package.json` با فیلد `n8n.{credentials:[...], nodes:[...]}` که به `dist/...js` اشاره می‌کند، `tsconfig.json` (`outDir dist`)، و `index.ts` خالی (n8n از فیلد package.json کشف می‌کند). _به‌جای `gulp` (که در sandbox به‌خاطر build نیتیو فریز شد و یک‌بار ResetSandbox لازم شد) از `copy-icons.js` بدون‌وابستگی برای کپی SVGها استفاده شد._ (ب) `credentials/AutomationBackendApi.credentials.ts`: `baseUrl`+`apiKey` (authenticate generic → هدر `x-api-key`)+`webhookSecret` اختیاری؛ `test`→`GET /me`. (ج) `nodes/AutomationBackend/AutomationBackend.node.ts` (action، ۴ عملیات): Run (`POST /run` + سوییچ `wait`→`?wait=true` + هدر `Idempotency-Key`)، Get Job (`GET /job/:u/:j`)، Schedule (`POST /schedule` + cron + name)، Cancel (`DELETE /cancel/:u/:j` + `closeBrowser`/`closeTab`)؛ با `helpers.httpRequestWithAuthentication` + `continueOnFail`؛ `parseSteps` (JSON متن یا آرایه). (د) `nodes/AutomationBackendTrigger/AutomationBackendTrigger.node.ts` (trigger، webhook POST): تأیید **HMAC** هدر `X-Signature` با **همان منطق `src/utils/signature.ts`** (constant-time، با/بدون پیشوند `sha256=`، گارد طول)، رد نامعتبر با ۴۰۱، فیلتر رویداد چندگانه؛ سازگاری متقابل امضا با بک‌اند با اسکریپت node تأیید شد (۵/۵). (ه) `README.md` (نصب GUI/npm/سورس + جدول‌ها + نکته‌ی CORS سمت‌سرور) + `examples/example-workflow.json` (Manual→Run(wait) + Trigger). **مهم برای آینده:** `n8n-workflow` به‌صورت **peerDependency** است (n8n در زمان اجرا می‌دهد)؛ نصبش در sandbox به‌خاطر transitive نیتیو `isolated-vm` (نیازمند C++ toolchain) شکست می‌خورد/فریز می‌کند — برای همین یک **type-shim محلی** `types/n8n-workflow.d.ts` (فقط زیرمجموعه‌ی API استفاده‌شده) افزوده شد تا `tsc` standalone کامپایل کند؛ نصب dev با `npm install --legacy-peer-deps` (فقط typescript+@types/node، بدون build نیتیو). وقتی n8n واقعی حاضر باشد، typeهای اصلی اولویت دارند. **تست:** `npm run build` پکیج **سبز** (dist منطبق با فیلد `n8n`)؛ سوییت بک‌اند بدون رگرسیون **۱۲۷** سبز. `dist/` و `node_modules/` پکیج gitignore شده‌اند (سراسری)؛ فقط سورس + `package-lock.json` کامیت شد.

- استپ ۱۷: **Workflow Storage (دسته G2)** _(۲۰۲۶-۰۶-۰۴)_. (الف) سرویس `src/services/workflow.service.ts` (`WorkflowService`، **CRLF**): CRUD + نسخه‌بندی روی Redis. مدل رکورد: `id` (تولید سرور `wf_<16hex>` با `randomBytes`)، `userId`, `name`, `description`, `steps`, `headless`, `webhookUrl`, `version`, `createdAt`, `updatedAt`. کلیدهای per-user در `src/utils/redis-keys.ts`: `getWorkflowKey`→`wf:meta:<u>:<id>`، `getUserWorkflowsKey`→`wf:index:<u>` (SET)، `getWorkflowVersionKey`→`wf:ver:<u>:<id>:<v>`، `getWorkflowVersionIndexKey`→`wf:verindex:<u>:<id>` (SET)، + `isValidWorkflowId` (`^[A-Za-z0-9_-]{1,64}$`). هر create/update یک snapshot نسخه ذخیره می‌کند؛ هرس خودکار به `config.WORKFLOW_MAX_VERSIONS` (پیش‌فرض ۲۰؛ ۰=نگه‌داشتن همه). (ب) endpointها در `src/Routes/user.routes.ts` (CRLF): `POST /workflows/:userId` (۲۰۱)، `GET /workflows/:userId` (لیست، newest-first)، `GET /workflows/:userId/:workflowId`، `PUT` (بامپ نسخه + snapshot)، `DELETE` (حذف + history)، `GET /workflows/:userId/:workflowId/versions` (newest-first)، و `POST /workflows/:userId/:workflowId/run` — **همان قرارداد `?wait=true` + `Idempotency-Key` مثل `/run`**؛ steps دوباره با plan فعلی اعتبارسنجی می‌شوند، job با `__workflowId`/`__workflowVersion` تگ می‌شود، و body می‌تواند headless/webhookUrl را برای همان run override کند. مالکیت با strict API-key binding از طریق `:userId` در مسیر (auth + blockCheck برای `/workflows` در `src/index.ts` افزوده شد). (ج) `workflowBodySchema` (Zod) در `src/schemas.ts`، نوع‌های `Workflow`/`WorkflowVersionSnapshot` در `src/types.ts` (CRLF)، کلید config `WORKFLOW_MAX_VERSIONS` در `src/config.ts`+`.env.example`. (د) مستند: ۵ مسیر `/workflows` + اسکیماهای `WorkflowBody`/`Workflow`/`WorkflowVersion` در `docs/openapi.yaml` (الان ۱۴ مسیر/۱۲ schema) + بخش «Saved Workflows» در `n8n-node/README.md` + README اصلی. **تست:** `tsc`/`build` سبز، `npm test`=**۱۵۰** (+۲۳: ۹ `workflow-service.test.ts` با Redis-stub درون‌حافظه‌ای شامل پرونینگ نسخه، ۱۰ `workflows.test.ts` route، ۴ تست workflow-key در `redis-keys.test.ts`). فایل‌های جدید TS `src/**` همه CRLF؛ تست‌ها/openapi همه LF طبق قرارداد.

- استپ ۱۸: **حالت Self-Hosted تک‌کاربره (`DEPLOYMENT_MODE`، دسته H)** _(۲۰۲۶-۰۶-۰۴)_. (الف) `src/config.ts` (**CRLF**): افزودن `import { randomBytes }`؛ `resolveDeploymentMode()` → `DEPLOYMENT_MODE` (`single`/`multi`، **پیش‌فرض `single`**)؛ `IS_SINGLE_USER`؛ `resolveApiToken()` → اگر `API_TOKEN` ست نشده و mode=`single` یک `tok_<48hex>` تصادفی تولید می‌کند (`API_TOKEN_AUTO_GENERATED`)؛ `FULL_ACCESS_PLAN` (`{quota:0, maxTabs:50, maxSteps:10000, priority:1, maxSchedules:1000, runLimit:0}`)؛ هشدارهای boot **mode-aware**: در `single` لاگ `🏠 DEPLOYMENT_MODE=single` و (اگر auto) چاپ یک‌بارهٔ توکن، در `multi` لاگ `🏢 ...` و هشدار `ADMIN_SECRET` پیش‌فرض. (ب) `src/core/UserManager.ts` (CRLF): `getEffectivePlan` در ابتدای تابع اگر `IS_SINGLE_USER` → کوتاه‌مدار به `FULL_ACCESS_PLAN` با `baseLevel:'single'` (بدون lookup در Redis)؛ `isUserBlocked` اگر `IS_SINGLE_USER` → `return false`. (ج) `src/middleware/auth.ts` (CRLF): `export const SINGLE_USER_ID='local'`؛ در `requireApiKey` پس از استخراج کلید (header/query/Bearer)، بلاک single-mode: نبود کلید→۴۰۱، کلید اشتباه یا `API_TOKEN` خالی→۴۰۳ (با ماسک‌کردن و warn)، درست→`apiKeyUserId=SINGLE_USER_ID` و `next()`. (د) `src/Routes/admin.routes.ts` (CRLF): پس از `router.use(requireAdminAuth)`، گارد single-mode که روی پیشوندهای `/set-user-level`,`/user/`,`/users/`,`/api-keys` پاسخ ۴۰۴ می‌دهد؛ endpointهای عملیاتی (stats/cleanup/reload-lua/restart-global-browser/system-restart/reset-quota) باز می‌مانند. (ه) `src/Routes/user.routes.ts` (CRLF): `GET /me` اکنون `mode: config.DEPLOYMENT_MODE` و `isSingleUser: config.IS_SINGLE_USER` را برمی‌گرداند و در single-mode `isAdmin=false`. (و) `.env.example` (CRLF): بخش «DEPLOYMENT MODE» با `DEPLOYMENT_MODE=single` + `API_TOKEN=`. **rate-limit:** `RATE_LIMIT_PER_MINUTE=120` پیش‌فرض سبک، در هر دو حالت فعال (`smartLimiter` روی `/run`,`/cancel`؛ `adminLimiter` روی `/admin`). **تست:** `tsc`/`build` سبز، `npm test`=**۱۵۷** (+۷: `tests/unit/single-user-mode.test.ts` — getEffectivePlan→full-access بدون لمس Redis، isUserBlocked→false، auth با توکن درست/x-api-key/نبود کلید(۴۰۱)/کلید اشتباه(۴۰۳)، و یک تست مسیر multi). ⚠️ تست‌های قدیمی `tests/integration/api.test.ts` چون مسیر **multi-tenant** auth (env-key admin + strict binding) را تست می‌کنند، با `process.env.DEPLOYMENT_MODE='multi'` در `tests/integration/setup.ts` پین شدند تا با پیش‌فرض جدید `single` نشکنند. ⚠️ **نکتهٔ مهم ویرایش CRLF:** نوشتن heredoc پایتونِ حاوی emoji (surrogate) باعث `UnicodeEncodeError` بعد از `open(p,'w')` می‌شود و فایل را **۰ بایت** می‌کند (config.ts یک‌بار این‌طور شد و با `git checkout` بازیابی شد) — راه‌حل: emojiها را به‌صورت escape (`'\\U0001f3e0'` و …) در یک فایل اسکریپت `.py` جدا بنویس و اجرا کن، نه heredoc. فایل تست single-user **LF**؛ همهٔ `src/**`+`.env.example` **CRLF**.

- استپ ۱۹: **اسکریپت نصب تعاملی `install.sh` + one-liner/Caddy/Coolify (دسته H3)** _(۲۰۲۶-۰۶-۰۴، گسترش‌یافته)_. سه فایل LF: `install.sh` (`chmod +x`)، `docker-compose.coolify.yml`، `Caddyfile.example`.
  - **نصب تک‌خطی:** `curl -fsSL https://raw.githubusercontent.com/Saeedkhoshafsar/plyr/main/install.sh | bash`. تابع `bootstrap_from_curl` تشخیص می‌دهد اسکریپت از فایل واقعی اجرا شده یا از pipe (با چک `${BASH_SOURCE[0]}` که هنگام `| bash` برابر `bash`/`/dev/stdin` یا خالی است). اگر pipe بود: نیاز به `git` را چک می‌کند، با `git clone --depth 1 --branch main` در `mktemp -d` کلون می‌کند، و `exec bash "$TMP/plyr/install.sh" "$@" </dev/tty` می‌زند تا `read`ها از کیبورد بخوانند (الگوی استاندارد Docker/nvm/rustup). ⚠️ بدون این re-exec، در حالت `| bash` پرامپت‌ها کار نمی‌کنند چون stdin خود pipe است.
  - **ویزارد:** خروجی رنگی TTY-aware، `set -euo pipefail`، توابع `confirm`(پیش‌فرض Y)/`ask`(مقدار پیش‌فرض)، `detect_pkg_mgr`(apt/dnf/yum/pacman/brew)، `maybe_sudo`، `gen_token`(`tok_`+48hex با node/openssl/urandom)، `sed_inplace`(GNU/BSD)، و `require_node`/`install_node_offer` که Node 20 را در صورت نبود نصب می‌کند.
  - **پنج هدف منو:** (۱) **Server (Node)** = `install_server_node` ویزارد ۶ مرحله‌ای `[1/6..6/6]`: deps(Node+Redis+npm) → Playwright(`install:browser`|`install:browser:deps`) → `ensure_env_file`(+تنظیم PORT دلخواه) → `npm run build` → دامنه/HTTPS → PM2(`ecosystem.config.js`+`pm2 save`+`pm2 startup` اختیاری). (۲) **Server (Docker)** = `install_server_docker` تشخیص compose v2/v1 + `up -d --build`. (۳) **Server (Coolify)** = `install_coolify` راهنمای گام‌به‌گام + اشاره به `docker-compose.coolify.yml` (بدون تغییر local). (۴) **Client (Chrome)** = `install_client` راهنمای Load-unpacked `extension/`. (۵) **Client (n8n)** = `install_client_n8n` build در `n8n-node/` + نصب اختیاری در `~/.n8n/custom`.
  - **دامنه + Caddy:** تابع `setup_caddy "$domain" "$port"` → چاپ راهنمای Cloudflare (رکورد A + **DNS only/پروکسی نارنجی خاموش**)، نصب Caddy (apt repo رسمی cloudsmith / dnf copr / brew)، ساخت `/etc/caddy/Caddyfile` از `Caddyfile.example` با جایگزینی `YOUR_DOMAIN`→دامنه و پورت، `systemctl reload caddy`. سپس `PANEL_URL=https://<domain>`.
  - **Coolify:** `docker-compose.coolify.yml` نسخهٔ ایزولهٔ compose — بدون `ports:` host (فقط `expose: 3000`؛ Traefik خود Coolify دامنه/TLS را هندل می‌کند)، با named volumes (redis-data/app-logs/app-profiles/app-uploads/app-downloads) برای persistence در redeploy، و env با پیش‌فرض `DEPLOYMENT_MODE=single`/`API_TOKEN=${API_TOKEN:-}`.
  - **خروجی نهایی:** `print_server_summary` کادر آدرس پنل (`PANEL_URL`) + `/health` + `API_TOKEN` (متغیر سراسری `TOKEN_OUT`) را چاپ می‌کند.
  - پرچم‌ها: `--server-node`/`--server-docker`/`--coolify`/`--client`/`--client-n8n`/`--domain <host>`/`--port <n>`/`-y|--yes`/`-h|--help`. اعتبارسنجی: `bash -n` سبز + **shellcheck (warning) تمیز** + تست منو/`--help`/`--coolify`. مستندسازی کامل در README. ⚠️ یادآوری CRLF: این فایل‌ها همگی LF هستند؛ هنگام ویرایش با ابزارهای CRLF-aware دقت شود.
- استپ ۲۰: **انتشار خودکار Docker image روی ghcr.io + راهنمای Coolify (Docker Image)** _(۲۰۲۶-۰۶-۰۴)_. دو فایل LF: `.github/workflows/docker-publish.yml` و `docs/COOLIFY.md`.
  - **GitHub Actions:** workflow «Build & Publish Docker image» با trigger روی `push` به `main` + tagهای `v*` + `workflow_dispatch`. مراحل: checkout → `docker/setup-buildx-action@v3` → `docker/login-action@v3` به `ghcr.io` با `secrets.GITHUB_TOKEN` (permission `packages: write`) → محاسبهٔ نام image با `tr '[:upper:]' '[:lower:]'` (الزام lowercase در ghcr) → `docker/metadata-action@v5` (تگ‌های `latest` روی default branch + `sha-<short>` + `vX.Y.Z` روی tag) → `docker/build-push-action@v6` (context `.`, file `./Dockerfile`, push، cache `type=gha mode=max`) → نوشتن خلاصه در `$GITHUB_STEP_SUMMARY`. image نهایی: `ghcr.io/saeedkhoshafsar/plyr:latest`.
  - **علت ساخت:** کاربر روی Coolify نوع منبع **Docker Image** را انتخاب کرده بود ولی آدرس سورس گیت‌هاب (`github.com/...`) را به‌عنوان image داده بود → Coolify image پیدا نکرد و **Exited** شد؛ همچنین `Ports Exposes` روی `80` بود نه `3000`. راه‌حل: ساخت image واقعی روی رجیستری + راهنمای درست.
  - **`docs/COOLIFY.md`:** راهنمای گام‌به‌گام روش Docker Image — (۰) ساخت و Public کردن package از Actions/ghcr، (۱) حذف منبع اشتباه از Danger Zone، (۲) ساخت **Redis جدا** (New→Database→Redis؛ چون Docker Image تنها Redis ندارد)، (۳) ساخت منبع Docker Image با `ghcr.io/saeedkhoshafsar/plyr:latest`، (۴) **Ports Exposes=3000** (نه 80)، (۵) Env: `DEPLOYMENT_MODE=single`/`NODE_ENV=production`/`PORT=3000`/`REDIS_URL=redis://<svc>:6379`/`API_TOKEN=`، (۶) دامنه + Cloudflare (A record + DNS only/پروکسی نارنجی خاموش)، (۷) Deploy. + جدول مقایسهٔ سه روش (Docker Image vs Compose vs Dockerfile) و یادآوری: اگر Compose با `docker-compose.coolify.yml` انتخاب شود، Redis خودکار می‌آید و نیازی به Redis جدا/`REDIS_URL` نیست.
  - README: بخش Coolify به دو راه (Compose آسان / Docker Image با ghcr.io) گسترش یافت + لینک به `docs/COOLIFY.md`.

- استپ ۲۱: **مدل دادهٔ یکنواخت آیتم‌محور (فاز نهایی، الهام مستقیم از n8n)** _(۲۰۲۶-۰۶-۰۶)_. (الف) ماژول مستقل و خالصِ `src/core/WorkflowItems.ts` (**CRLF**، بدون وابستگی، قابل‌تست ایزوله): قرارداد `WorkflowItem = { json: Record<string,unknown>, binary?: Record<string,WorkflowBinary> }` + helperها: `emptyItem`/`emptyStream` (شروع با یک آیتم خالی مثل n8n)، `isWorkflowItem` (guard)، `toItem` (شیء→json با کپی، primitive/آرایه→`{value}`)، `normalizeToItems` (null/undefined→`[]`، شیء→۱ آیتم، آرایه→n آیتم fan-out، primitive→۱ آیتم)، `resolveOutputStream` (pass-through جریان قبلی وقتی استپ خروجی ندارد)، `itemsToJson`، و `summarizeItems` (خلاصهٔ count+sample با سقف ~۸KB برای کانال زنده). (ب) `src/types.ts` (CRLF): `import type { WorkflowItem }`؛ `StepOutput` با `inputItemCount`/`outputItemCount`/`outputSample`/`outputTruncated`؛ `AutomationContext` با `items: WorkflowItem[]` (جریان آیتم) و `nodeOutputs: Record<string, WorkflowItem[]>` (حافظهٔ خروجی هر نود برای `$node["x"].json` آینده). (ج) `src/pipeline.ts` (CRLF): import helperها؛ init `items: emptyStream()` و `nodeOutputs: {}` در context؛ در مسیر موفقیتِ هر استپ، `result` با `normalizeToItems` به آیتم تبدیل و جریان به‌روز می‌شود (یا pass-through)، زیر کلید `saveAs`/`action#index` در `nodeOutputs` ذخیره می‌شود، و متادیتای آیتم به `StepOutput` و رویداد `step.done` ضمیمه می‌گردد. **سازگاری عقب‌رو کامل:** `variables`/`set_variable`/`extract`/`saveAs` دست‌نخورده — مدل آیتم یک **لایهٔ اضافه** است نه جایگزین. (د) **تست:** `tests/unit/workflow-items.test.ts` (**LF**، ۲۳ تست: قرارداد/guard، toItem کپی، normalize برای شیء/primitive/آرایه/آرایهٔ خالی، fan-out n-آیتمی، pass-through، itemsToJson، summarize با cap اندازه). `tsc`/`build` سبز، `npm test`=**۱۸۰** (۱۵۷→۱۸۰). اسکریپت‌های throwaway پایتونِ CRLF بعد از اعمال **پاک شدند** (در ریپو کامیت نشدند).

- استپ ۲۲: **کتابخانهٔ چند-ورکفلو در UI + اتصال به CRUD موجود** _(۲۰۲۶-۰۶-۰۶)_. (همه فایل‌ها **LF** — UI سمت کلاینت). (الف) `public/js/api.js`: تابع `put(path, body, opts)` + قرارداد ورکفلو: `wfBase(userId)`→`/workflows/:userId`، `listWorkflows`/`getWorkflow`/`createWorkflow`(POST)/`updateWorkflow`(PUT با بامپ نسخه)/`deleteWorkflow`(DELETE)/`listWorkflowVersions`(`/:id/versions`)/`runWorkflow`(`/:id/run`) — همگی در `window.API` export شدند (شامل `put`). (ب) `public/js/flow-editor.js`: متغیر `currentWorkflow` + متدهای `openWorkflow(meta, steps)` (ست‌کردن زمینه + `loadSteps`)، `newWorkflow()` (پاک‌کردن زمینه + گراف نو)، `getCurrentWorkflow`/`setCurrentWorkflow`. (ج) `public/js/views.js`: `renderWorkflows(root)` گرید کارت با عملیات کامل (ساخت/تغییرنام/کپی/حذف/اجرا/باز در ادیتور/تاریخچهٔ نسخه+restore) همه به API.* وصل؛ هلپر `cssId(id)` برای querySelector امن؛ `pendingWorkflowToOpen` برای انتقال ورکفلوی انتخابی به ادیتور؛ نوار ابزار ادیتور دکمهٔ «💾 ذخیره روی سرور» (PUT اگر `currentWorkflow.id` باشد وگرنه prompt نام + POST) و برچسب `#fe-wf-label`. (د) `public/js/app.js`: `'workflows'` به `ROUTES`. (ه) `public/index.html`: آیتم ناوبری `#/workflows`. (و) `public/js/i18n.js`: i18n کامل fa+en — `nav.workflows` + `fe.saveServer`/`fe.unsaved` + بلوک `wf.*` (۲۸ کلید در هر زبان). (ز) `public/css/styles.css`: کلاس‌های `.wf-grid/.wf-card/.wf-card-head/.wf-name/.wf-desc/.wf-meta/.wf-actions/.wf-versions/.wf-ver-row` + هلپر عمومی `.small`. **تصمیم طراحی:** سرور فقط `steps[]` اجراییِ canonical را ذخیره می‌کند؛ ادیتور هنگام load چیدمان خطی تمیز را از `steps[]` بازسازی می‌کند (موقعیت نود سمت سرور ذخیره نمی‌شود — به استپ ۲۳ موکول شد). CRUD سمت سرور از قبل با `tests/integration/workflows.test.ts` + `tests/unit/workflow-service.test.ts` (در ۱۸۰ تست) پوشش‌داده شده. `tsc`/`build` سبز، `npm test`=**۱۸۰**. (هیچ تغییری در `src/**` نبود؛ بدون اسکریپت CRLF/throwaway.)
- استپ ۲۳: **بازطراحی بصری Canvas نودبیس (هم‌حسِ n8n، بدون کپی کد — لایسنس AGPL)** _(۲۰۲۶-۰۶-۰۶)_. (همه فایل‌ها **LF** — UI سمت کلاینت + تست). (الف) `public/js/actions.js`: افزودن `cat` به هر ۱۸ اکشن (goto/wait=navigation؛ click/hover/scroll/fill/type/press/select/upload=interaction؛ extract/export-data/screenshot/variable=data؛ cookie/clipboard/notification/log=integration) + آرایهٔ `CATEGORIES` (navigation/interaction/data/flow/integration/trigger با رنگ هگز و لیبل i18n `cat.*`) + `categoryById(id)` (fallback به `other`) — همگی در `window.ACTION_CATALOG` export شدند. (ب) `public/js/flow-editor.js` (بازنویسی عمدهٔ Step 23): `GRID=20`+`snap()`، grid-snap با جابه‌جایی آزاد روی `Alt`؛ drag&drop از palette به کانواس (`text/ab-action` در dataTransfer + `placeNewNode`)؛ انتخاب چندتایی `selSet` + `activeSelection()`+جابه‌جایی گروهی؛ box-select (`Shift`+درگ روی پس‌زمینه: `boxRect/applyBoxSelection/renderBoxSelect`)؛ کپی/پیست (`copySelection/pasteClipboard` با ذخیرهٔ نودها بر اساس ایندکس نسبی + یال‌های داخلی) با `Ctrl+C`/`Ctrl+V`، انتخاب‌همه `Ctrl+A`، حذف `Delete`/`Backspace` (`removeSelection`)؛ minimap (`renderMinimap` در ۱۶۸×۱۱۲ + کلیک=هم‌مرکزسازی) و کنترل‌های zoom/fit (`buildOverlay`→data-z in/out/fit؛ `zoomBy/zoomIn/zoomOut/fitToScreen/nodesBBox/updateZoomLabel`)؛ palette با جست‌وجوی نود (`renderPalette/renderPaletteList/paletteItem` گروه‌بندی بر اساس ترتیب `CATEGORIES`)؛ **API حالت بصری نود** `setNodeStatus(ref,status)`/`clearStatuses()`/`chainNodeIds()` (ref=nodeId یا ایندکس زنجیره؛ status=idle/running/success/error — زیرساخت UI، دادهٔ واقعی در استپ ۲۶). API عمومی جدید: `fitToScreen/zoomIn/zoomOut/setNodeStatus/clearStatuses` (بقیهٔ API دست‌نخورده). (ج) `public/css/styles.css`: `--cat-color`+`border-inline-start` نوار لهجه روی `.flow-node`؛ هاله‌های وضعیت `.status-running`(`@keyframes fe-pulse`)/`.status-success`/`.status-error`+`.fn-status`(`@keyframes fe-blink`)؛ `.palette-search/.palette-group/.palette-item`(cursor:grab)؛ `.fe-boxselect`؛ `.fe-zoom-ctrl/.fe-zbtn/.fe-zoom-label`؛ `.fe-minimap/.mm-node/.mm-viewport` (مخفی در ≤۹۸۰px). (د) `public/js/i18n.js`: ۱۲ کلید جدید fa+en (`fe.searchNode/noNodes/zoomIn/zoomOut/fit` + `cat.navigation/interaction/data/flow/integration/trigger/other`) با برابری fa/en + به‌روزرسانی `fe.hint`. (ه) **تست:** `tests/unit/action-catalog.test.ts` (**LF**، ۹ تست DOM-free — بارگذاری `actions.js` با شیم `window` از طریق `node:vm`؛ بدون افزودن jsdom: حضور ACTIONS/CATEGORIES/helperها، cat روی هر اکشن، نگاشت cat به CATEGORY تعریف‌شده، فرمت id/رنگ‌هگز/لیبل، categoryById شناخته/ناشناخته→other، یکتایی idها، icon+fields هر اکشن، resolve/fallback `actionById`). `tsc`/`build` سبز، `npm test`=**۱۸۹** (۱۸۰+۹)، smoke مرورگر روی `:8099` با PlaywrightConsoleCapture=۰ خطای کنسول. **تصمیم:** `flow-editor.js` به‌خاطر وابستگی سنگین به DOM (بدون jsdom) با smoke مرورگر پوشش داده شد نه unit؛ موقعیت نودها هنوز سمت سرور persist نمی‌شود (به استپ ۲۴ موکول شد). (هیچ تغییری در `src/**` نبود؛ اسکریپت throwaway `/tmp/add_cat.py` برای افزودن CRLF-safe `cat` به اکشن‌ها بعد از اعمال **پاک شد**، در ریپو کامیت نشد.)

**باگ‌های ثبت‌شده که هنوز باز/بعداً:** بخش «دسته‌ها» در `PLAN.md` را ببین. (دسته‌های A–H همگی در استپ‌های ۱–۲۰ پوشش داده شدند؛ فقط backlog اولویت‌پایین [E4] Triggerهای متنوع و [E5] Marketplace باز است.)

---

## ۶) چک‌لیست پایان هر استپ (کپی-پیست)

```text
[ ] کد تغییر کرد و تست/typecheck سبز است (npx tsc --noEmit)
[ ] اگر باگ پیچیده‌ی جدید دیدی: در PLAN.md ثبت + استپ اولویت‌دار اضافه شد
[ ] PLAN.md: استپ فعلی [x] و تاریخ خورد
[ ] SESSION_RECOVERY.md بخش «وضعیت فعلی» به‌روز شد
[ ] git add -A && commit پیام واضح && git push origin main
[ ] گزارش ۳-خطی + لینک ریپو داده شد
[ ] منتظر «ادامه» ماند
```
