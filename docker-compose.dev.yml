# Disposable, loopback-only development stack. Never use this file for production.
# ./plyr dev-docker owns only the "plyr-dev" Compose project and deletes its volumes.
services:
  redis:
    image: redis:7-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  app:
    image: plyr-dev:local
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      redis:
        condition: service_healthy
    environment:
      NODE_ENV: production
      APP_ENV: server
      DEPLOYMENT_MODE: single
      API_TOKEN: admin123
      REDIS_URL: redis://redis:6379
      PORT: "3000"
      PUBLIC_DOMAIN: http://localhost:3000
      WORKFLOW_STORAGE_ROOT: /app/workflow-files
    ports:
      - "127.0.0.1:3000:3000"
    shm_size: "1gb"
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://127.0.0.1:3000/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))"]
      interval: 10s
      timeout: 5s
      start_period: 30s
      retries: 6
